## 1. 实现
- [x] 1.1 为 `merge_pdfs.py` 新增 `--nup-rows`、`--nup-cols` 参数，并调整 `--pages-per-sheet` 逻辑，允许二者组合定义每张纸的网格。
- [x] 1.2 根据行列参数计算单元格，保持页面顺序与打印类似（行优先、支持竖排），并对无效参数提供清晰错误提示。
- [x] 1.3 更新帮助文本和验证逻辑，确保默认情况下仍与旧行为一致。

## 2. 验证
- [x] 2.1 使用示例 PDF 测试不同组合（如 `--pages-per-sheet 2 --nup-rows 2 --nup-cols 1`）生成对应布局，人工核查排版。
- [x] 2.2 验证错误输入（0、负数、不匹配等）或仅指定其中一项时提示信息正确。
