## 1. 实现
- [x] 1.1 编写 `merge_pdfs.py`，解析命令行参数并调用 `PyPDF2.PdfMerger` 按顺序合并多个 PDF。
- [x] 1.2 增加输入检查与错误信息（如文件缺失、输出路径目录不存在等），并在 README/脚本注释中说明使用方式。

## 2. 验证
- [x] 2.1 使用示例 PDF（如 `1.pdf`, `2.pdf`）运行脚本，确认生成的合并文件包含全部页面且顺序正确。
