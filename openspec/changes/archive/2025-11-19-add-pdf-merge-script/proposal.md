## Why
- 目前项目缺少一个可以将多个 PDF 合并为单个文件的工具脚本。
- 用户需要一个简单的命令行方式来把多份 PDF 顺序写入到一个输出文件中，方便批量处理。

## What Changes
- 新增一个 `merge_pdfs.py` 脚本，支持通过命令行传入输出文件及若干输入 PDF，并将它们按顺序合并。
- 提供基础的输入校验和错误提示，确保缺少参数或源文件不存在时能给出明确信息。

## Impact
- 无需改动现有代码，新增脚本独立运行。
- 引入对 `PyPDF2` 的依赖（由用户自行安装），不会影响其它部分。
