## 1. 实现
- [x] 1.1 更新 `merge_pdfs.py` 支持 `--input-dir` 扫描目录下全部 PDF 并按文件名排序。
- [x] 1.2 与现有文件列表参数兼容，至少要求其中一种输入方式有效。
- [x] 1.3 为无匹配文件或目录不存在等场景提供明确错误提示。

## 2. 验证
- [x] 2.1 构造包含多份 PDF 的目录，运行目录模式确认生成顺序正确的合并文件。
- [x] 2.2 分别测试目录空、目录不存在、与 `--overwrite` 交互等情况，确认错误输出符合预期。
