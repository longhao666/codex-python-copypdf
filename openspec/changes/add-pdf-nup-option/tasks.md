## 1. 实现
- [x] 1.1 更新 `merge_pdfs.py` 的参数解析，新增 `--pages-per-sheet`（默认 1，必须为正整数）。
- [x] 1.2 在合并流程中读取页面尺寸，按 `pages-per-sheet` 计算子页面布局（行列数接近平方），缩放并绘制到输出页。
- [x] 1.3 确保 `--input-dir` 与显式文件列表均可使用该功能，并为参数非法或页面缺失提供清晰错误。

## 2. 验证
- [x] 2.1 使用示例 PDF 测试 1-up（默认）、2-up、4-up 等场景，检查生成的输出页数量与布局正确。
- [x] 2.2 验证 `--pages-per-sheet` 为 0、负数或大于可用页面时的提示符合预期。
